version: '3'
services:
  api:
    entrypoint: start-ce
    image: cenv:latest
    restart: always
    environment:
      - POSTGRES_HOST=data
      - POSTGRES_PORT=5432
    env_file:
      - /var/local/cenv/django.env
      - /var/local/cenv/pg.credentials.env
    ports:
      - "5123:5123"
  data:
    image: postgres:11
    restart: always
    env_file: /var/local/cenv/pg.credentials.env
    volumes:
      - /var/local/cenv:/var/lib/postgresql
    ports:
      - "5432:5432"
