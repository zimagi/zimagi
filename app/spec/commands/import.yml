command:
    import:
        base: import
        priority: 60
        parameters:
            import_names:
                parser: variables
                type: str
                default: []
                optional: true
                help: "one or more import specification names"
            ignore_requirements:
                parser: flag
                flag: "--ignore-req"
                help: "ignore requirements when not listed in import_names"
        parse:
            import_names:
            ignore_requirements:
