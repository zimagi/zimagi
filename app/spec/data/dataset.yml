data:
    dataset:
        class: DataSet
        base: resource
        mixins: [provider, group]
        roles:
            edit: [data-admin]
            view: [data-auditor]
        meta:
            dynamic_fields: [data]
            provider_name: dataset
            command_base: data
