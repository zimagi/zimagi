data:
  dataset:
    class: DataSet
    base: name_resource
    mixins: [provider, group]
    roles:
      edit: [data-admin]
      view: [data-auditor]
    meta:
      dynamic_fields: [data]
      provider_name: dataset
      command_base: data
