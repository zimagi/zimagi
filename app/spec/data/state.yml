data:
    state:
        roles:
            edit: [ config_admin ]
            view: [ config_auditor ]
            search: [ config_auditor ]
        data:
            base: environment
            fields:
                value:
                    type: encrypted_data
                    options:
                        "null": true
        command:
            base: action
            priority: 95
            server_enabled: true
            options:
                allow_update: false
