data:
    state:
        class: State
        base: name_resource
        roles:
            edit: [config-admin]
            view: [config-auditor]
        fields:
            value:
                type: "@zimagi.EncryptedDataField"
                color: encrypted
                options:
                    "null": true
