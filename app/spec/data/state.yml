data:
    state:
        roles:
            edit: [ config_admin ]
            view: [ config_auditor ]
        data:
            class: State
            base: environment
            fields:
                value:
                    type: "@fields.EncryptedDataField"
                    color: encrypted
                    options:
                        "null": true
