data:
    state:
        class: State
        base: resource
        roles:
            edit: [config-admin]
            view: [config-auditor]
        fields:
            value:
                type: "@fields.EncryptedDataField"
                color: encrypted
                options:
                    "null": true
