data:
    state:
        class: State
        base: environment
        roles:
            edit: [ config_admin ]
            view: [ config_auditor ]
        fields:
            value:
                type: "@fields.EncryptedDataField"
                color: encrypted
                options:
                    "null": true
