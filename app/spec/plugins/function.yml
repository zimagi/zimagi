plugin:
    function:
        base: base
        interface:
            exec:
                params: "*"
                returns: "*"
        providers:
            keys:
            values:
            value:
            prefix:
            join:
            lstrip:
            rstrip:
            substitute:
            time:
