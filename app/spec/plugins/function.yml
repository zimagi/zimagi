plugin:
    function:
        base: base
        interface:
            exec:
                params: "*"
                returns: "*"
        providers:
            keys:
            values:
            value:
