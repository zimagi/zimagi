plugin:
    encoder:
        base: base
        interface:
            get_dimension:
                returns: int
            encode:
                params:
                    sentences: list
                returns: list
        option:
            device:
                type: str
                help: "Device to run encoding process"
                default: "cuda:0"

        providers:
            transformer: