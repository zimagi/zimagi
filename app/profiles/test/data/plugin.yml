parents:
    - test/data/basic

run:
    check_null_provider:
        requires: "#prefix(@instances, save_)"
        command: "@command_base list"
        instance_search_query:
            - "provider_type__is_null=True"
        reverse_status: true
