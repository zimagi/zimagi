variables:
    data_name:
        help: Name of data model
        required: true
    field_name:
        help: Name of dictionary field on data model
        required: true

map:
    spec.yml:
        target: spec/data/<{ data_name }>.yml
        location: data.<{ data_name }>.fields.<{ field_name }>
