type: "@django.ForeignKey"
relation: <{ related_data_name }>
color: relation
options:
    "null": <{ nullable }>
    on_delete: "@django.<{ on_delete }>"
    editable: <{ editable }>
    system: <{ not editable }>
#%- if reverse_related_name %#
    related_name: "<{ reverse_related_name }>"
#%- endif %#
