
# Cluster provisioning requirements

ansible>=2.5.0,!=2.7.0 # Configuration management system
jinja2>=2.9.6          # Templating engine used by Ansible
netaddr==0.7.19        # Network address manipulation
pbr>=1.6               # Injects default behaviors into setuptools run
passlib==1.7.1         # Password hashing library

# Orchestration application requirements

django==2.1.3          # Django web framework (used as CLI framework)
django-db-mutex==1.1.0 # Django mutex locking
pandas==0.23.4         # Data structures for tabular data
pytz==2018.5           # Timezones
titlecase==0.12.0      # String processing
PyYAML==3.12           # YAML parsing/generation
psutil==5.4.6          # System utilities
tblib==1.3.2           # Traceback serialization (for parallel exceptions)
tabulate==0.8.2        # Pretty print tabular data
terminaltables==3.1.0  # Table rendering in the console
