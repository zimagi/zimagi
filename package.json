{
  "name": "zimagi",
  "engines": {
    "node": "^18.17.1 || ^20.3.0 || >= 21.0.0"
  },
  "scripts": {
    "check:prettier": "prettier --check .",
    "fix:prettier": "prettier -w ."
  },
  "dependencies": {
    "prettier": "^3.5.3"
  }
}
